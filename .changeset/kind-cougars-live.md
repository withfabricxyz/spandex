---
"@spandex/core": minor
"@spandex/react": minor
---

Enable deferred getQuote and getQuotes action via proxy with control over which methods run where. Added helper method for using spanDEX edge as a proxy.
