# Getting started

## Overview

SMAL makes token swap integration easy for developers by providing a simple and consistent interface to interact with multiple decentralized exchanges via multiple aggregators. This provides redundancy, better pricing, and expanded liquidity for token swaps.

## Installation

To install SMAL, run the following command:

:::code-group

```bash [npm]
# Quoting only
npm i @withfabic/smal

# Quotes with onchain simulation (recommended)
npm i viem @withfabric/smal @withfabric/smal-simulation
```

```bash [bun]
# Quoting only
bun i @withfabic/smal

# Quotes with onchain simulation (recommended)
bun i viem @withfabric/smal @withfabric/smal-simulation
```

```bash [pnpm]
# Quoting only
pnpm i @withfabic/smal

# Quotes with onchain simulation (recommended)
pnpm i viem @withfabric/smal @withfabric/smal-simulation
```

:::

## Example

```ts twoslash
// [!include ~/snippets/example.ts]
```