name: Site building
on:
  pull_request:

jobs:
  build:
    name: Build site
    runs-on: ubuntu-slim

    steps:
      - name: Clone repository
        uses: actions/checkout@v4
      - name: Setup
        uses: ./.github/actions/setup
      - name: Build site
        working-directory: ./site
        run: bun run build
