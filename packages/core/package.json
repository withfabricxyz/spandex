{
  "name": "@withfabric/swap-quoter",
  "version": "0.1.0",
  "description": "A library for quoting swaps on various DEXs via aggregators",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build index.ts --outdir=dist --target=es2024 --external:typescript",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.4",
    "@types/bun": "latest",
    "viem": "^2.37.7"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
